---
phase: 02-paper-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/monitor/wallet-monitor.ts
  - src/data/supabase.ts
  - src/core/runner.ts
  - .paul/STATE.md
autonomous: false
---

<objective>
Goal: Make the paper trading loop fully observable — every trade the leader makes is detected, processed through confirmation, executed in paper mode, and persisted to Supabase with enough data to evaluate performance against the 14-day targets.

Purpose: Validate that PATS-Copy can autonomously copy a top Polymarket trader for 14 days, hitting >65% win rate, >8% monthly return, and <15% drawdown before live capital is committed.

Output: A running system that populates copy_trades and daily_performance in Supabase after every leader trade cycle.
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@src/monitor/wallet-monitor.ts
@src/data/supabase.ts
@src/core/runner.ts
</context>

<acceptance_criteria>

AC-1: Trade detection pipeline fires on new leader trades
  Given the leader wallet (0x2a2C53bD) makes a new trade on Polymarket
  When WalletMonitor polls the Data API within 30 seconds
  Then a "WalletMonitor: New leader trade →" log line appears with correct market, side, and price

AC-2: Confirmation layer evaluates every detected trade
  Given a new leader trade is emitted
  When ConfirmationLayer.confirm() runs
  Then logs show either "Confirmation APPROVED", "Confirmation VETOED", or "Confirmation SKIPPED" — never silent

AC-3: Paper execution records the trade
  Given a trade is approved or skipped (not vetoed)
  When CopyExecutor runs in paper mode
  Then PaperTradingEngine records the position and balance updates

AC-4: Supabase persists every trade attempt
  Given any trade reaches the runner's handleLeaderTrade()
  When the result is returned from CopyExecutor
  Then a row appears in copy_trades with correct status ('open', 'vetoed', or 'skipped')

AC-5: Daily performance rolls over at midnight
  Given a day boundary is crossed
  When handleDayRollover() fires
  Then a row appears in daily_performance with pnl, win_rate, and trades_executed for that date

AC-6: System runs unattended for 14 days
  Given the bot is started
  When left running without intervention
  Then no fatal crashes, Glint stays connected (or auto-reconnects), leaderboard polls every 5min

</acceptance_criteria>

<tasks>

<task id="T1">
  files: [src/monitor/wallet-monitor.ts]
  action: >
    Verify the timestamp fix is working on real live trades. Add a single log line in
    emitLeaderTrade() that prints the raw t.timestamp and t.created_at values alongside
    the normalised ISO result. This confirms toISOTimestamp() is parsing correctly
    when the first real trade comes through.
  verify: >
    When the next leader trade arrives, log shows:
    "WalletMonitor: trade ts raw=[X] created_at=[Y] → [ISO string]"
    and the ISO string is a 2024/2025/2026 date, not 1970.
  done: AC-1 verified on first real leader trade
</task>

<task id="T2">
  files: [src/data/supabase.ts]
  action: >
    Add a startup connectivity test. In initSupabase(), after creating the client,
    run a lightweight probe: SELECT 1 from leaders LIMIT 1. Log success or failure
    with a clear message. This catches schema issues immediately at boot rather than
    silently failing on first trade.
  verify: >
    On restart, logs show one of:
    "Supabase: schema OK — leaders table confirmed" or
    "Supabase: schema check FAILED — [reason]"
  done: AC-4 precondition verified — schema confirmed at boot
</task>

<task id="T3">
  files: [src/core/runner.ts]
  action: >
    Fix the silent insertCopyTrade call for skipped trades. Currently the runner
    calls db.insertCopyTrade() even when result.success=false and decision='skipped'.
    Gate the Supabase insert so only 'approved' and 'vetoed' decisions are persisted
    (not 'skipped' — those are noise from the confirmation age check, not real trade
    decisions). Also add result logging: "Supabase: copy trade saved [id]" on success.
  verify: >
    After a skipped trade: no insertCopyTrade call in logs.
    After an approved/vetoed trade: "Supabase: copy trade saved [uuid]" appears.
  done: AC-4 — only meaningful trade records written to Supabase
</task>

</tasks>

<boundaries>
DO NOT CHANGE:
- The confirmation layer logic or veto thresholds
- The proportional sizing formula in copy-executor.ts
- The hysteresis rotation margins in selector.ts
- The leaderboard scraper strategy order
- Any .env values

SCOPE LIMITS:
- T1 is diagnostic only — one log line, no logic change
- T2 is a read-only probe — no writes to Supabase
- T3 gates one conditional — minimum code change
</boundaries>

<verification>
1. Restart bot after changes — confirm "Supabase: schema OK" at boot
2. Wait for first leader trade — confirm timestamp log shows 2024/2025/2026 ISO date
3. Confirm skipped trades no longer trigger insertCopyTrade
4. Check Supabase dashboard: copy_trades table receives rows for approved/vetoed only
</verification>

<success_criteria>
- [ ] AC-1: First real leader trade detected with correct timestamp
- [ ] AC-2: Confirmation log appears for every detected trade
- [ ] AC-3: Paper balance updates after first executed trade
- [ ] AC-4: copy_trades row written to Supabase for approved/vetoed trades
- [ ] AC-5: daily_performance row written at next midnight rollover
- [ ] AC-6: Bot runs 14 days without manual intervention
</success_criteria>

<output>
.paul/phases/02-paper-validation/02-01-SUMMARY.md (created after UNIFY)
</output>
